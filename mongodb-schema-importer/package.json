{
  "name": "mongodb-schema-importer-generator",
  "version": "1.0.0",
  "description": "A simple app that creates a Flatfile importer based off of a given mongodb schema",
  "scripts": {
    "start": "node index.js",
    "mongod": "mongod --dbpath=data --port 28015",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "http://github.com/monkeydzeke",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.17.3",
    "mongodb": "~3.0.8",
    "mongoose": "^5.2.14"
  },
  "devDependencies": {}
}
